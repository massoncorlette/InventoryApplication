{
  "name": "inventoryapplication",
  "version": "1.0.0",
  "description": "Focusing on MVC Pattern structure, GET/POST requests from form to DB, Handling data requests with async, handling data dynamically for templated view.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "node db/populatedb.js",
    "start": "node app.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format:css": "prettier --write \"**/*.css\"",
    "format:js": "prettier --write \"**/*.{js,jsx}\"",
    "format": "npm run format:css && npm run format:js",
    "check:css": "prettier --check \"**/*.css\"",
    "check:js": "prettier --check \"**/*.{js,jsx}\"",
    "check": "npm run check:css && npm run check:js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "express-validator": "^7.2.1",
    "lint": "^0.8.19",
    "pg": "^8.16.0",
    "serve-favicon": "^2.5.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.23.0",
    "eslint": "^9.24.0",
    "globals": "^16.0.0",
    "prettier": "3.5.3"
  }
}
